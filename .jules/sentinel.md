## 2025-04-11 - Fixed Time-Of-Check to Time-Of-Use (TOCTOU) Vulnerabilities in File Operations
**Vulnerability:** Several places in the codebase checked if a file or directory existed before attempting to delete it using `.exists()` followed by `fs::remove_file()`. This introduces a TOCTOU race condition where the file state could change between the check and the actual removal, potentially causing unexpected failures or unauthorized operations in shared environments.
**Learning:** Using `.exists()` before an operation is an anti-pattern for concurrent environments. The file system state can change concurrently, invalidating the result of the prior `.exists()` check.
**Prevention:** Avoid checking `.exists()` prior to file system manipulations like remove. Instead, perform the operation directly (e.g., `fs::remove_file()`) and explicitly handle `ErrorKind::NotFound` or other expected errors.

## 2024-04-05 - Path Traversal via Unvalidated Configuration Values
**Vulnerability:** Path traversal (CWE-22) due to constructing file paths using unvalidated `provider_name` strings fetched from the application configuration. In files like `src/core/chat.rs`, `src/cli/config.rs`, and `src/cli/models.rs`, file paths were constructed via `format!("models/{}.json", provider_name)`. While `provider_name` originates from an internal configuration, allowing it to contain path separators (`/`, `\`) or parent references (`..`) can lead to arbitrary file reads or writes if a malicious or malformed configuration entry is loaded.
**Learning:** Internal configuration values, even if they seem trusted or are usually validated at the point of entry, must always be treated as untrusted data when used in sensitive operations like file path construction. A defense-in-depth approach is required: validate at the point of entry AND sanitize/validate at the point of use. Relying on the assumption that "the config is safe" creates architectural blind spots.
**Prevention:** Always validate and sanitize variables used to construct file paths just prior to the file system operation. Check for directory traversal sequences (`..`) and path separators (`/`, `\`), or restrict values to a strict alphanumeric pattern, regardless of the variable's source.
